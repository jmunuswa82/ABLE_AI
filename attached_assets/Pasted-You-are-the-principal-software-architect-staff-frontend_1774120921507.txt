You are the principal software architect, staff frontend engineer, staff backend engineer, product engineer, QA architect, design systems engineer, motion engineer, and performance specialist for this project.

Your task is to IMPLEMENT, REIMPLEMENT, REDESIGN, REFACTOR, HARDEN, and COMPLETE the entire product based on the already imported Figma design in this Replit workspace.

You must not behave like a prototype assistant.
You must not stop at surface styling.
You must not leave partial flows.
You must not leave broken UI states.
You must not preserve weak logic just because it already exists.
You must rebuild any fragile part properly.

==================================================
MISSION
==================================================

Turn the current project into a fully working, advanced, production-grade application where:

- the imported Figma design is translated faithfully into a polished real product
- every major screen is implemented
- every feature flow works end to end
- all states are handled properly
- the app is responsive on desktop and mobile
- nothing is fake
- nothing is placeholder-only
- nothing is visually broken
- nothing is logically broken
- nothing is disconnected from real state or real data flow

This is not a visual mockup task.
This is not a design-only task.
This is a full implementation task.

==================================================
PRIMARY OBJECTIVE
==================================================

Rebuild the app so it feels like a premium, highly advanced startup product with:

- strong frontend architecture
- real feature logic
- real navigation flows
- resilient state management
- polished interactions
- stable async handling
- high-performance rendering
- accurate validation
- clean component abstraction
- reusable design system primitives
- robust loading, empty, success, warning, and error states

If existing code is messy, incomplete, redundant, or fragile, replace it with better architecture.

==================================================
FIGMA IMPLEMENTATION REQUIREMENT
==================================================

The Figma design is already imported.
You must use it as the visual source of truth.

Your responsibilities:

1. audit the imported Figma-derived code and structure
2. identify weak/generated/bloated code patterns
3. replace bad structure with maintainable architecture
4. preserve visual fidelity while improving implementation quality
5. convert static design fragments into real reusable components
6. make layouts responsive and production-grade
7. connect screens, actions, state, and business logic properly

Do not just “clean up a little.”
Do a serious rebuild where needed.

==================================================
NON-NEGOTIABLE ENGINEERING RULES
==================================================

You must:

- implement real working logic
- fix root causes, not symptoms
- preserve strong type safety
- remove duplicated UI logic
- remove dead code
- remove unused props and unstable state
- normalize component APIs
- implement proper loading and error handling
- make forms and user actions robust
- ensure navigation is consistent
- ensure all controls do something meaningful
- ensure mobile and desktop behavior are both strong
- ensure code is maintainable and scalable

You must not:

- leave TODOs in critical paths
- leave fake buttons
- leave dead tabs
- leave broken navigation
- leave disconnected modals
- leave placeholder charts or fake data in real screens unless clearly isolated as mock/demo
- use hacky patches when proper refactoring is needed
- silence errors without fixing them
- create giant monolithic components when modular architecture is better
- degrade the imported design into generic UI

==================================================
PRODUCT QUALITY BAR
==================================================

The product must feel:

- premium
- robust
- coherent
- fast
- elegant
- trustworthy
- technically mature
- startup-grade
- investor-demo ready
- production-leaning, not prototype-ish

==================================================
IMPLEMENTATION PROCESS
==================================================

You must execute in this order:

PHASE 1 — DEEP AUDIT
- inspect all imported Figma screens and components
- inspect routing, shared UI, state, forms, async flows, data services, and utilities
- identify:
  - broken screens
  - duplicated components
  - weak generated code
  - missing states
  - disconnected actions
  - responsiveness issues
  - animation issues
  - styling inconsistency
  - bad accessibility
  - poor performance hotspots
  - broken business logic
  - incomplete user flows

PHASE 2 — ARCHITECTURE REBUILD
- create a clean scalable app structure
- split into:
  - app shell
  - route modules
  - reusable UI primitives
  - feature modules
  - state/store layer
  - service/data layer
  - utility/helpers
  - animation layer
  - validation layer
- refactor large generated files into maintainable feature-oriented components

PHASE 3 — DESIGN SYSTEM HARDENING
- extract reusable primitives from imported Figma code
- create consistent:
  - buttons
  - inputs
  - dropdowns
  - tabs
  - modals
  - drawers
  - toasts
  - cards
  - tables/lists
  - empty states
  - skeletons
  - badges
  - status chips
  - tooltips
  - command surfaces
- unify spacing, sizing, typography, surfaces, hover, focus, pressed, disabled, selected, and loading states

PHASE 4 — FEATURE IMPLEMENTATION
Implement every screen and user flow with real logic.
Anything currently static must become functional.

Required:
- real routing and navigation
- real state transitions
- real forms and validation
- real filters, sort, search, pagination if needed
- real modal open/close and submit flows
- real empty/error/success/loading states
- real settings persistence where applicable
- real panels, inspectors, and detail views
- real multi-step flows where required
- real interactive controls
- real responsive layout adaptation

PHASE 5 — RESPONSIVE REBUILD
- optimize for desktop first
- then make tablet and mobile excellent
- do not simply stack everything badly
- ensure:
  - adaptive nav
  - collapsed panels
  - proper touch targets
  - readable density
  - stable layout at all breakpoints
  - no overflow bugs
  - no clipped modals
  - no broken fixed/sticky positioning
  - no impossible-to-use timeline/grid/chart components on mobile

PHASE 6 — ADVANCED UX + MOTION
- preserve Figma visual quality but improve the interaction model
- add polished, purposeful motion
- use advanced motion only where it improves UX
- implement:
  - page transitions
  - panel transitions
  - modal transitions
  - hover/selection transitions
  - progressive disclosure
  - skeleton loading
  - state feedback
- motion must be smooth, intentional, and not distracting

PHASE 7 — ROBUSTNESS + QA HARDENING
- test all flows
- remove broken interactions
- remove orphaned UI
- validate edge cases
- validate invalid input paths
- validate async race conditions
- validate loading behavior
- validate error recovery
- validate mobile
- validate responsiveness
- validate dark theme consistency if used
- validate keyboard navigation and focus handling

==================================================
ADVANCED LOGIC REQUIREMENTS
==================================================

Implement advanced-level logic, not superficial UI.

Where relevant, add:
- centralized state management with clear ownership
- derived selectors/computed state
- optimistic UI only when safe
- debounced search/filtering
- request cancellation / stale request handling
- memoization for expensive rendering
- virtualization for large lists/tables/timelines
- schema-based validation
- error boundaries
- reusable async hooks/services
- consistent API/data adapters
- serialization/deserialization guards
- stable identifiers for lists and panel selection
- proper sync between route params and UI state
- persisted user preferences if appropriate
- undo/redo patterns where useful
- keyboard shortcuts where useful
- analytics/event hooks only if meaningful and non-invasive

==================================================
UI/UX REQUIREMENTS
==================================================

Every screen must support:

- initial state
- loading state
- empty state
- populated state
- error state
- retry/recovery state
- success confirmation state where needed

Every actionable element must:
- have a real purpose
- visibly respond to interaction
- be wired correctly
- not dead-end the user

Every complex screen must:
- have hierarchy
- have discoverable controls
- have contextual help or labels where needed
- avoid clutter
- support expert usage without confusing first-time users

==================================================
PERFORMANCE REQUIREMENTS
==================================================

Optimize for:
- fast initial render
- low unnecessary re-renders
- stable animation performance
- efficient large-data rendering
- efficient image/icon usage
- minimized layout thrash
- clean dependency boundaries

Refactor anything performance-heavy or Figma-generated and bloated.

==================================================
CODE QUALITY REQUIREMENTS
==================================================

Enforce:
- clear file structure
- clear naming
- small focused components
- reusable hooks/utilities
- strict typing
- zero sloppy any-casting unless absolutely unavoidable and justified
- removal of dead code and duplicate branches
- clear comments only where logic is non-obvious
- consistent architecture throughout

==================================================
REBUILD PRIORITY
==================================================

Highest priority:
1. app shell and navigation
2. imported Figma screen fidelity
3. feature logic wiring
4. state management correctness
5. responsive behavior
6. advanced interactions
7. error handling and stability
8. performance tuning
9. polish and refinement

==================================================
DELIVERABLE EXPECTATION
==================================================

Do not give only plans.
Do not stop after partial edits.
Do not stop after styling.
Do not stop after scaffolding.

You must:
- inspect the existing project
- identify all major issues
- rewrite fragile parts
- implement missing logic
- reconnect all flows
- make the imported design real
- ensure nothing important is broken

Continue until the project is coherent, stable, polished, and feature-complete.

==================================================
FINAL HARDENING PASS
==================================================

After implementation, do one full hardening sweep focused on:

- broken logic
- screen-to-screen flow gaps
- responsiveness bugs
- modal/drawer issues
- state synchronization issues
- invalid loading states
- empty states
- error recovery
- component API consistency
- animation polish
- visual fidelity to Figma
- advanced UX refinement
- code maintainability

==================================================
SUCCESS CRITERIA
==================================================

The task is complete only when:

- all imported Figma screens are implemented properly
- all important flows work end to end
- the app is responsive on desktop and mobile
- the UI feels premium and coherent
- major edge cases are handled
- no critical interaction is broken
- no major placeholder logic remains
- code is strong enough for ongoing product development

Start by auditing the existing imported Figma implementation and rebuild the app properly from there. Do not ask to continue. Continue through implementation, refactor, wiring, responsiveness, hardening, and polish until the app is solid.